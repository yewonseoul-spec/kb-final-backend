<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.scoula.notification.mapper.NotificationMapper">

    <select id="findByMemberNo"
            resultType="org.scoula.notification.dto.NotificationResDTO">
        SELECT n.noti_no,
               n.noti_type,
               n.ref_no,
               n.content,
               n.is_read,
               n.created_at,
               CASE WHEN n.noti_type = 'DEADLINE'
                        THEN DATEDIFF(b.apply_end_date, CURDATE())
                   END AS d_day
        FROM notification n
                 LEFT JOIN benefit b
                           ON b.benefit_no = n.ref_no
                               AND n.noti_type = 'DEADLINE'
        WHERE n.member_no = #{memberNo}
        ORDER BY n.created_at DESC, n.noti_no DESC
    </select>

    <select id="countUnread" resultType="int">
        SELECT COUNT(*)
        FROM notification
        WHERE member_no = #{memberNo}
          AND is_read = 'N'
    </select>

    <insert id="insert">
        INSERT INTO notification (member_no, noti_type, ref_no, content)
        VALUES (#{memberNo}, #{notiType}, #{refNo}, #{content})
    </insert>

    <update id="markRead">
        UPDATE notification
        SET is_read = 'Y'
        WHERE noti_no = #{notiNo}
          AND member_no = #{memberNo}
    </update>

    <update id="markAllRead">
        UPDATE notification
        SET is_read = 'Y'
        WHERE member_no = #{memberNo}
          AND is_read = 'N'
    </update>

    <delete id="delete">
        DELETE FROM notification
        WHERE noti_no = #{notiNo}
          AND member_no = #{memberNo}
    </delete>

</mapper>