<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.scoula.home.mapper.HomeMapper">

    <!-- 마감된 혜택이 '인기'로 뜨면 안 되므로 진행 중인 것만 -->
    <select id="findPopularBenefits" resultType="org.scoula.home.dto.PopularBenefitDTO">
        SELECT
            b.benefit_no AS benefitNo,
            b.plcy_nm AS plcyNm,
            bc.category_name AS categoryName,
            b.sprvsn_inst_cd_nm AS sprvsnInstCdNm,
            b.inq_cnt AS inqCnt

        FROM benefit b
                 JOIN benefit_category bc
                      ON b.category_code = bc.category_code

        WHERE b.is_active = 'Y'
          AND (
            b.apply_end_date IS NULL
                OR b.apply_end_date &gt;= CURDATE()
            )

        ORDER BY b.inq_cnt DESC, b.benefit_no DESC
            LIMIT #{limit}
    </select>

</mapper>