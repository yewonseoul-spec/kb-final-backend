<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.scoula.member.mapper.MemberMapper">

    <resultMap id="memberMap" type="org.scoula.security.account.domain.MemberVO">
        <id property="memberNo" column="member_no"/>
        <result property="loginId" column="login_id"/>
        <result property="password" column="password"/>
        <result property="email" column="email"/>
        <result property="role" column="role"/>
        <result property="realName" column="real_name"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="get" resultMap="memberMap">
        SELECT member_no, login_id, password, email, role,
               real_name, status, created_at, updated_at
        FROM member
        where login_id = #{loginId}
    </select>

    <select id="findByLoginId" resultMap="memberMap">
        SELECT member_no, login_id, password, email, role,
               real_name, status, created_at, updated_at
        FROM member
        WHERE login_id = #{loginId}
    </select>
    <select id="countByEmail" resultType="int">
        SELECT COUNT(*)
        FROM member
        WHERE email = #{email}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="memberNo">
        INSERT INTO member (login_id, password, email, real_name)
        VALUES (#{loginId}, #{password}, #{email}, #{realName})
    </insert>

    <update id="update">
        UPDATE member
        SET email       = #{email},
            update_date = now()
        WHERE login_id = #{loginId}
    </update>

    <update id="updatePassword">
        UPDATE member
        SET password    = #{newPassword},
            update_date = now()
        WHERE login_id = #{loginId}
    </update>
    <update id="withdraw">
        UPDATE member
        SET status     = 'N',
            updated_at = NOW()
        WHERE member_no = #{memberNo}
          AND status = 'Y'
    </update>

</mapper>
